import datetime
import random
import time
from decimal import Rounded
from tokenize import NEWLINE







class TypeEngine:
    def __init__(self, text):
        self.target = text
        self.current_input = ""
        self.start_time = None
        self.end_time = None
        self.total_key_strokes = 0 
        self.missed_key = 0
        self.score = 0 
        self.target = 0 




    def get_user_answer(self, chosen_sentence):
        start_time = time.time()
        while score < target:
            answer = input("Touch Type: ")

        if answer == sentence:
            score += 1
        else:
            print("Incorrect")

        end_time = time.time()








